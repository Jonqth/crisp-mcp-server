# Crisp Marketplace plugin credentials
CRISP_IDENTIFIER=your_plugin_identifier
CRISP_KEY=your_plugin_key
CRISP_WEBSITE_ID=your_website_id

# Transport: "stdio" (local/Claude Desktop) or "http" (Railway/remote)
MCP_TRANSPORT=http

# Port for HTTP mode (Railway sets this automatically)
PORT=3000
